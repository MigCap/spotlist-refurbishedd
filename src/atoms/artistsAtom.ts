import { atom } from 'recoil';

export const topArtistsState: any = atom({
  key: 'topArtistsState',
  default: null,
});

export const artistState: any = atom({
  key: 'artistState',
  default: null,
});
